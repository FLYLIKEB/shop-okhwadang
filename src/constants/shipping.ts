// Temporary production smoke-test policy mirror.
// Runtime cart/checkout totals use /api/shipping/quote; these constants are only fallback values.
export const FREE_SHIPPING_THRESHOLD = 10;
export const SHIPPING_FEE = 3000;
